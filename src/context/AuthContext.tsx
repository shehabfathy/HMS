// import { createContext } from "react";

// // This type definition might also live here or in a separate types file
// type LoginData = {
//   _id: string;
//   role: "admin" | "user";
//   verified: boolean;
//   iat: number;
//   exp: number;
// };

// type AuthContextType = {
//   loginData: LoginData | null;
//   logOut: () => void;
//   getUser: () => void;
// };

// export const AuthContext = createContext<AuthContextType | null>(null);
